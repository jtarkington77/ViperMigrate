<UserControl x:Class="ViperMigrate.App.Views.ProgressView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:conv="clr-namespace:ViperMigrate.App.Converters">
    <Grid Margin="24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <!-- Header -->
        <StackPanel Grid.Row="0" Margin="0,0,0,24">
            <TextBlock Text="Processing..."
                       Style="{StaticResource Heading}" />
            <TextBlock Text="{Binding CurrentOperation}"
                       Style="{StaticResource SubHeading}"
                       Margin="0,4,0,0" />
        </StackPanel>

        <!-- Overall Progress -->
        <ProgressBar Grid.Row="1" Value="{Binding OverallProgress}"
                     Minimum="0" Maximum="100"
                     Height="8" Margin="0,0,0,24"
                     Foreground="{StaticResource AccentBrush}"
                     Background="{StaticResource SurfaceBrush}" />

        <!-- Staged View (when Stages has items) -->
        <ScrollViewer Grid.Row="2" VerticalScrollBarVisibility="Auto"
                      Visibility="{Binding Stages.Count, Converter={StaticResource CountToVisibility}}">
            <ItemsControl ItemsSource="{Binding Stages}">
                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <Border Margin="0,0,0,12">
                            <StackPanel>
                                <!-- Stage Header -->
                                <Border Background="{StaticResource SecondaryBrush}"
                                        CornerRadius="4,4,0,0" Padding="12,8">
                                    <Grid>
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="*" />
                                            <ColumnDefinition Width="Auto" />
                                        </Grid.ColumnDefinitions>
                                        <TextBlock Grid.Column="0"
                                                   Foreground="{StaticResource TextBrush}"
                                                   FontWeight="Bold" FontSize="14">
                                            <Run Text="Stage " /><Run Text="{Binding Stage, Mode=OneWay, StringFormat='{}{0:D}'}" /><Run Text=": " /><Run Text="{Binding StageName, Mode=OneWay}" />
                                        </TextBlock>
                                        <TextBlock Grid.Column="1"
                                                   Text="{Binding StatusText}"
                                                   Foreground="{Binding Status, Converter={StaticResource StageStatusToColorConverter}}"
                                                   FontWeight="SemiBold"
                                                   VerticalAlignment="Center" />
                                    </Grid>
                                </Border>

                                <!-- Category Items within Stage -->
                                <ItemsControl ItemsSource="{Binding Items}">
                                    <ItemsControl.ItemTemplate>
                                        <DataTemplate>
                                            <Border Background="{StaticResource SurfaceBrush}"
                                                    Padding="16,6,12,6" Margin="0,0,0,1">
                                                <Grid>
                                                    <Grid.ColumnDefinitions>
                                                        <ColumnDefinition Width="*" />
                                                        <ColumnDefinition Width="Auto" />
                                                    </Grid.ColumnDefinitions>
                                                    <Grid.RowDefinitions>
                                                        <RowDefinition Height="Auto" />
                                                        <RowDefinition Height="Auto" />
                                                    </Grid.RowDefinitions>
                                                    <TextBlock Grid.Column="0" Grid.Row="0"
                                                               Text="{Binding Category}"
                                                               Foreground="{StaticResource TextBrush}"
                                                               VerticalAlignment="Center" />
                                                    <TextBlock Grid.Column="1" Grid.Row="0"
                                                               Text="{Binding StatusText}"
                                                               Foreground="{Binding Status, Converter={StaticResource StatusToColorConverter}}"
                                                               FontWeight="SemiBold"
                                                               VerticalAlignment="Center" />
                                                    <TextBlock Grid.Column="0" Grid.ColumnSpan="2" Grid.Row="1"
                                                               Text="{Binding Detail}"
                                                               Foreground="{StaticResource SubtextBrush}"
                                                               FontSize="11"
                                                               Visibility="{Binding Detail, Converter={StaticResource StringToVisibility}}"
                                                               Margin="0,2,0,0" />
                                                </Grid>
                                            </Border>
                                        </DataTemplate>
                                    </ItemsControl.ItemTemplate>
                                </ItemsControl>
                            </StackPanel>
                        </Border>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>
        </ScrollViewer>

        <!-- Flat View (capture mode — when Stages is empty) -->
        <ScrollViewer Grid.Row="2" VerticalScrollBarVisibility="Auto"
                      Visibility="{Binding Stages.Count, Converter={StaticResource CountToCollapsed}}">
            <ItemsControl ItemsSource="{Binding Items}">
                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <Border Background="{StaticResource SurfaceBrush}"
                                CornerRadius="4" Padding="12,8" Margin="0,0,0,4">
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*" />
                                    <ColumnDefinition Width="Auto" />
                                </Grid.ColumnDefinitions>
                                <TextBlock Grid.Column="0" Text="{Binding Category}"
                                           Foreground="{StaticResource TextBrush}"
                                           VerticalAlignment="Center" />
                                <TextBlock Grid.Column="1" Text="{Binding StatusText}"
                                           Foreground="{Binding Status, Converter={StaticResource StatusToColorConverter}}"
                                           FontWeight="SemiBold"
                                           VerticalAlignment="Center" />
                            </Grid>
                        </Border>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>
        </ScrollViewer>
    </Grid>
</UserControl>
